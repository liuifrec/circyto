[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "circyto"
version = "0.2.0"
description = "circyto.py — CIRI-full wrapper for scRNA-seq circRNA quantification to Loom/H5AD."
readme = "README.md"
license = {text = "MIT"}
authors = [{name="James Liu", email="example@example.com"}]
dependencies = [
  "typer>=0.12.0",
  "rich>=13.0.0",
  "pysam>=0.22.0",
  "pandas>=2.0.0",
  "numpy>=1.24.0",
  "scipy>=1.10.0",
  "loompy>=3.0.7",
  "anndata>=0.10.5",
  "joblib>=1.3.0"
]

[project.scripts]
circyto = "circyto.cli.circyto:app"
